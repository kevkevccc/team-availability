// Shared data storage — Firebase Realtime Database URL (no trailing slash),
// e.g. 'https://robotics-availability-default-rtdb.firebaseio.com'
// Empty string = not connected yet; the app shows a warning banner.
const DB_URL = '';
